# テスト用のTerraform変数設定ファイル
# このファイルはGitHub Actions マトリックステストで使用されます

repository = {
  name = "test-repo-BRANCH_STRATEGY_PLACEHOLDER"
  description = "Test repository for BRANCH_STRATEGY_PLACEHOLDER strategy"  
  branch_strategy = "BRANCH_STRATEGY_PLACEHOLDER"
}

# カスタムREADME.md内容（オプション）
# 指定しない場合はデフォルトのREADMEが作成される
readme_content = <<EOF
# Test Repository

これは**BRANCH_STRATEGY_PLACEHOLDER**戦略を使用したテストリポジトリです。

## ブランチ戦略

- ブランチ戦略: BRANCH_STRATEGY_PLACEHOLDER

## 使用方法

このリポジトリはTerraformで自動作成されました。

## 注意事項

- これはテスト用のリポジトリです
- 本番環境では使用しないでください
EOF

# ブランチ保護設定（オプション）
branch_protection_settings = {
  required_reviews           = 1
  dismiss_stale_reviews      = true
  require_code_owner_reviews = false
  enforce_admins            = false
  required_status_checks    = []
}